#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    char name[1000];
   fgets(name,sizeof(name),stdin);
  int    l = strlen(name)-1;

    int i,j;
   while(name[i] != '\0')
    {
       
    } 
    return 0;
}
